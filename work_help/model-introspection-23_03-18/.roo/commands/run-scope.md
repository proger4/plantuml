Run introspection pipeline for one root model.

Args: {{args}}

Instructions:
- Do not run hidden steps.
- Run deterministic wrapper only:

python .roo/scripts/roo_run_scope.py {{args}}

- Return run id, step counts, and key artifact paths.
